{
  "name": "document-server",
  "version": "1.0.0",
  "description": "A REST API server that allows conversion between *docx and *.html files.",
  "main": "index.js",
  "scripts": {
    "watch": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "html-to-docx": "^1.1.34",
    "mammoth": "^1.4.16",
    "multer": "^1.4.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
